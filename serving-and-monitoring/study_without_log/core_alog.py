# core_alog.py
# 服务的核心代码
def run_algo(x: float, y: float, z: float) -> float:
    """
    你的核心算法逻辑，尽量保持“纯粹”：输入 -> 输出，不依赖全局状态
    """
    # 这里写你的真实算法
    result = x * 2 + y * 3 + z * 4
    return result